main: ${PACKAGE}.Main
name: ${NAME}
version: "${VERSION}"
api-version: "1.21"
author: walker84837
folia-supported: true
description: "CustomRecipes - QOL upgrades that improve unused items by adding recipes to them."
